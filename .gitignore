# secrets
.env

# python
.venv/
__pycache__/
*.pyc

# node / next
frontend/node_modules/
frontend/.next/

# generated runs -- but TRACK the committed replay run so the demo works offline
runs/*
!runs/demo_committed/
